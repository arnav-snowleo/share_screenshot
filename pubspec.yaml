name: share_screenshot
description: take screenshot of screen and share.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  screenshot: ^1.2.3
  share: ^2.0.4
  #image_gallery_saver: ^1.7.0
  #permission_handler: ^8.1.6
  path_provider: 

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  